require 'spec_helper'

describe User do
  pending "add some examples to (or delete) #{__FILE__}"

  it "Should only allow the user to view their own plays" do
  	pending
  end

  it "Should only allow user to edit their own plays" do
  	pending
  end

  it "Should not let user create more than six plays per week" do
  	pending
  end

  it "Should not let the user make the same selection twice" do
  	pending
  end

end

